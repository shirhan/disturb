@model Disturb.Models.Delivery

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Delivery</legend>

        @Html.HiddenFor(model => model.DeliveryId)

        <div class="editor-label">
            @Html.LabelFor(model => model.DeliviryDate)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.DeliviryDate)
            @Html.ValidationMessageFor(model => model.DeliviryDate)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Provider)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Provider)
            @Html.ValidationMessageFor(model => model.Provider)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
